# Program for checking whether string Start with "st" or checking if its end with "rt"
puts "Enter string to check if it starts with 'st'"
	STstr=gets
        if STstr=~/\A(st)/
		puts "Yess it does start with 'st'"
	else
		puts "No it does'nt"
	end
puts "Enter string to check if it ends with 'rt'"
	Endstr=gets
	if Endstr=~/(art)$/
		puts "Yess it does "
	else
		puts "No it does'nt"
	end
